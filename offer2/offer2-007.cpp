#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

class Solution {
public:
    vector<vector<int>> threeSum(vector<int>& nums) {
        // 排好序，先拿一个元素，然后在右边的数组里找，也就成了target问题了
        // 不是返回下标，所以事情变得简单了
        // 需要去重，在右边找两个的target不能前后两次一样，否则得到的结果是一样的
    }
};

int main() {

    Solution res;
    
    return 0;
}
